<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}YenCraft{% endblock %}</title>
    {% block styles %}  {# Tailwindcss CDN para todas las páginas que extiendan de base.html.twig #} 
        <script src="https://cdn.tailwindcss.com"></script>
    {% endblock %}

</head>
<body>
    <header class="bg-gray-800 text-white">
        <nav class="container mx-auto p-4 flex justify-between">
            <a href="{{ path('main_index') }}" class="text-lg font-bold hover:text-green-400">Inicio</a>
            <a href="{{ path('main_tienda') }}" class="hover:text-green-400">Tienda</a>
            <a href="{{ path('main_blog') }}" class="hover:text-green-400">Blog</a>
        </nav>
    </header>

    <main>
        {% block content %}{% endblock %}
    </main>
    {% block scripts %}{% endblock %}
</body>
</html>
